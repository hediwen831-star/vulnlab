<!--
  XXE · medium 档的「外部 DTD」

  这个文件扮演的是**攻击者自己服务器上的 DTD 文件**。
  现实里它位于攻击者控制的域名下，这里放在靶场目录里，
  只是为了让整个绕过过程能在本地完整复现，不依赖外部服务器。

  ⚠️ 注意这是一个真正的 DTD 文件，不是 PHP 脚本 ——
     解析器读取的是它的**原始内容**，所以这里不能出现任何 PHP 标签，
     否则整份 DTD 会因为语法不合法而加载失败。

  ── 它为什么能绕过 medium 档的黑名单 ──

  实体声明写在【这里】而不是请求体里，所以：

    · 过滤器检查请求体 → 只看到 `<!DOCTYPE order SYSTEM "attacker.dtd">`
      → 没有 `<!ENTITY` 字样 → 放行

    · 解析器加载这个 DTD → 拿到下面的实体声明 → 展开 `&xxe;`
      → 读出 xxe_probe.txt 的内容

  `xxe_probe.txt` 是相对路径 —— 相对于**这份 DTD 所在的位置**解析，
  也就是同一个目录下的那个演示文件。
-->
<!ENTITY xxe SYSTEM "xxe_probe.txt">
