# 安全政策

## ⚠️ 首先，也是最重要的：这个项目的漏洞是故意的

**VulnLab 是一个漏洞靶场。** 它包含 SQL 注入、XSS、文件上传、SSRF、命令注入
等**刻意构造**的漏洞 —— 这是项目的全部意义，不是疏忽。

所以：

- **不要为这些故意漏洞提交安全报告或申请 CVE**
  （举例：`html/sqli/low.php` 存在 SQL 注入 —— 是的，这是设计好的）
- **不要把本靶场部署到公网**
- **不要在未隔离的环境中运行「上传 getshell」「命令注入」这类场景**

---

## 那什么才是真正的安全问题

有一类问题尤其值得报告，即不在设计范围内的：

| 类型 | 例子 |
|---|---|
| **靶场逃逸** | 某个场景能读到靶场目录以外的文件、或影响到宿主机 |
| **非预期的影响范围** | 某个「只读」场景实际能写文件、能持久化 |
| **环境泄漏** | 靶场意外暴露了真实凭据或真实配置 |
| **CI / 构建链问题** | 工作流可被注入、依赖被投毒 |
| **文档与实现不符** | 文档说「已修复」，实际还能打通 |

最后一条尤其值得报。

如果绕过了某个 high 档（文档和 CI 都声称它打不动），那说明我们的
回归断言设计有缺陷 —— 这比「多一个漏洞」有价值得多。

---

## 报告方式

请通过 GitHub 的 [Private vulnerability reporting](../../security/advisories/new)
提交，或发邮件到 hediwen831@gmail.com。

**请不要开公开 issue** —— 即使对靶场项目，公开一个「逃逸类」问题也可能
让正在用它练习的人处于风险中。

---

## 部署建议（给使用者）

本靶场设计为在**本地隔离环境**中使用：

- 用 `php -S 127.0.0.1:8080`（只绑回环），或用 Docker（端口也只映射到回环）
- 不要改成监听 `0.0.0.0` 后暴露到局域网
- 用完即关
- `html/uploads/` 里可能有测试时上传的文件，定期清理
  （`.gitignore` 已排除该目录，不会被提交）

---

## 已知的、有意的风险点

以下行为是**设计如此**，使用前请知悉：

| 位置 | 行为 | 说明 |
|---|---|---|
| `html/upload/low.php` | 上传的 `.php` 会被真实执行 | 三个上传场景里最危险的一个 |
| `html/cmdi/*.php` | 会真实执行系统命令 | 用了 `shell_exec`，注入后能执行任意命令 |
| `html/ssrf/low.php` | 会真实发起服务端请求 | 包括 `file://` 协议读本地文件 |
| `html/internal/inner-service.php` | 会返回假的「内部凭据」 | 内容是伪造的，不是真实凭据 |

这几个场景请只在本地靶场里运行。

---

## 支持范围

| 版本 | 支持状态 |
|---|---|
| `main` 分支最新提交 | 接受报告 |
| 历史提交 | 请先确认能在最新 `main` 上复现 |
