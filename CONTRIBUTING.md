# 贡献指南

## 开发环境

只需要 PHP 7.2+（含 `pdo_sqlite` 扩展）：

```bash
git clone https://github.com/hediwen831-star/vulnlab.git
cd vulnlab
./start.sh          # Windows: start.bat
```

打开 <http://127.0.0.1:8080> 即可。数据会自动初始化，不需要建库导数据。

> 没有 PHP？用 Docker：`docker compose up`

## 提交前必须跑的两件事

```bash
# 1. PHP 语法检查
find html -name "*.php" -exec php -l {} \;

# 2. 安全属性回归（需要靶场已启动）
python tests/verify_lab.py
```

两条都必须通过。CI 会跑同样的检查（外加 PHP 7.3 / 7.4 / 8.1 三版本矩阵）。

---

## 硬性约定

### 1. 每个漏洞场景必须凑齐四件套

**只加「有漏洞的页面」不算完成。** 必须包括：

| # | 内容 | 说明 |
|---|---|---|
| ① | 三档源码 | `low.php` / `medium.php` / `high.php` |
| ② | Writeup | `writeups/<场景>.md`，含推导过程与防御清单 |
| ③ | 机读 PoC | `pocs/*.yaml`，能被扫描器消费 |
| ④ | 修复对照 | high 档就是「应该怎么写」 |

**第 ④ 项最容易被忽略，但它才是靶场价值的一半** ——
只教怎么打、不教怎么修，学到的是攻击技巧而不是安全认知。

而「打不动」的 high 档本身就是一种资产：它证明了修复方式确实有效。
CI 有专门的断言守护这批资产不被改坏。

### 2. 新场景必须加 CI 断言

在 `tests/verify_lab.py` 的 `build_checks()` 里加。至少覆盖三类：

- low 档的漏洞场景**确实能打通**（否则场景坏了没人知道）
- medium 档的绕过点**确实能绕过**
- high 档**打不动**，且**不牺牲功能**

最后一类特别重要：**防止有人为了「修得更安全」把功能改坏。**
安全修复不该以牺牲功能为代价 —— 这是很常见的过度修复。

### 3. 只能用 PHP 7.2+ 的语法和函数

靶场声明兼容 PHP 7.2+，**不要用高版本才有的东西**：

| 不能用 | 引入版本 | 替代写法 |
|---|---|---|
| `str_starts_with()` | 8.0 | `strpos($x, $p) === 0` |
| `str_contains()` | 8.0 | `strpos($x, $n) !== false` |
| 箭头函数 `fn() =>` | 7.4 | 传统闭包 `function () use () {}` |
| `?->` nullsafe | 8.0 | 显式 `null` 判断 |
| `match` 表达式 | 8.0 | `switch` |

**为什么这条要单独强调**：用了高版本的语法/函数，页面会**白屏**
（那一块内容整体消失），不报错、不告警，而 `php -l` 完全正常 ——
因为「函数不存在」是**运行时**错误，语法检查抓不到。

开发过程中真的踩到两次（`str_starts_with` 和箭头函数），
所以现在有一条「全站页面健康检查」断言兜底。

### 4. 不要引入真实风险

靶场里的漏洞是**受控的**。新增场景时注意：

- 不要让它能影响靶场目录以外的文件
- 不要引入需要真实外网连接的场景（既依赖环境，也可能变成攻击跳板）
- 危险场景（命令注入、上传 getshell 等）要在页面上加醒目提示

### 5. 记录实测数据与平台差异

Writeup 里要写**实际跑出来的结果**，不要只写理论。

尤其要记录**平台差异** —— 这个项目里已经踩到好几处：

| 场景 | 实测差异 |
|---|---|
| SSRF | `127.1` / 十六进制等 IP 变形写法：Linux 有效，Windows 无效 |
| 命令注入 | cmd 不认 `;`、不支持换行作分隔符；`&` 才是 cmd 的主分隔符 |

**「payload 有效性依赖目标环境」本身就是知识点**，值得写下来 ——
真实渗透中盲试一堆 payload 既浪费时间又容易触发告警。

---

## 提交规范

用 [Conventional Commits](https://www.conventionalcommits.org/)：

| 前缀 | 用途 |
|---|---|
| `feat:` | 新场景 / 新档位 |
| `fix:` | 修复（含「修复某个档位被改坏」） |
| `docs:` | Writeup / README |
| `test:` | CI 断言 |
| `ci:` | 工作流 |
| `chore:` | 杂项 |

---

## Pull Request

- 一个 PR 只做一个场景（或一件事）
- 描述里写清楚：**新增了什么档位、怎么验证的、实测结果是什么**
- CI 必须全绿
- 新场景要同步更新 README 的漏洞矩阵

---

## 报告问题

- **Bug / 新场景建议** → [开 issue](.github/ISSUE_TEMPLATE/)
- **「我绕过了某个 high 档」** → 非常欢迎！
  这说明我们的回归断言有缺陷，比「多一个漏洞」有价值得多
- **靶场逃逸类问题** → 见 [SECURITY.md](SECURITY.md)，**不要开公开 issue**
