# 更新日志

本文件记录本项目所有值得注意的变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 新增

场景由 5 个扩充至 12 个。每个场景仍配齐四件套：三档源码、Writeup、机读 PoC、
修复对照。

**漏洞场景**

| 场景 | low | medium 的绕过点 | high 的修复 |
|---|---|---|---|
| 文件包含（LFI） | 路径穿越读任意文件 | 黑名单单次替换 `../` → 双写 `....//` 绕过 | 白名单映射 + 越界兜底 |
| PHP 反序列化 | 任意类 + 任意属性 | 类名黑名单 → 小写 `cache` 绕过（PHP 类名不区分大小写） | `allowed_classes` 白名单 |
| XXE | 内联实体读文件 | 实体声明挪到外部 DTD → 过滤器看不见但解析器会加载 | 禁外部实体加载器 + 不传 `LIBXML_NOENT` + 输入收窄 |
| JWT | `alg:none` 被当成「无签名可校验 → 放行」 | 算法判断写对了，但密钥是 `secret` → 爆破后伪造签名 | 算法白名单 + 高熵密钥（环境变量）+ `exp` 校验 |
| CSRF | 零防护 | 只查 `Referer` → 攻击者可用 `no-referrer` 去掉它，或用含目标域名的自己人域名通过子串匹配 | 会话绑定的一次性 token + `Referer` 作为补充 |
| SSTI | 表达式直接 `eval` | 黑名单挡括号 → 反引号运算符 `` `cmd` `` 绕过 | 不做表达式求值，改为变量白名单 |
| 越权（水平 / IDOR） | 查询里没有归属条件 | 归属校验写对了，但「我是谁」来自请求参数 `uid` | 身份只从服务端取 + 归属条件进查询 |

12 个场景的绕过方式各异，但归结为同一条规律：**防护所依据的信息，如果是
请求方可以自由决定的，就无法约束请求方。**

| 场景 | 防护依赖的信息 | 为何不可靠 |
|---|---|---|
| SQL 注入 | 删掉关键字后的文本 | 删除动作把片段拼了回来 |
| 文件包含 | 删掉 `../` 后的路径 | 同上 |
| 反序列化 | 字符串里的类名 | PHP 查找类时不区分大小写 |
| XXE | 请求体里的文本 | 文档与外部 DTD 才是解析器实际的输入 |
| JWT | `alg` 字段 | 算法由 token 自身声明 |
| CSRF | `Referer` | 客户端可以自愿不发，域名也可以自愿包含 |
| SSTI | 括号 | 反引号运算符不需要括号 |
| 越权 | `uid` 请求参数 | 身份由请求方自己声明 |

**基础设施**

- `html/lib/paths.php`：计算并在页面上显示「当前工作目录到目标目录的相对路径」。
  PHP 内置服务器会把工作目录切到被请求脚本所在目录，相对路径写错时
  `@file_put_contents` 会静默失败，页面只显示「没有新文件」。
- `html/csrf/state.php`、`html/idor/store.php`：场景状态与演示数据，含模拟的
  当前登录用户。

**CI 安全属性回归**：21 条 → 78 条

每个新场景都配了对照组、绕过点与反向保护三类断言。其中几条的设计取舍如下：

- XXE 使用靶场自带的固定文件作为判据，而非 `/etc/passwd` 或 `win.ini`。
  后者不保证在每台机器上存在，会让断言变成对环境的判断。
- JWT 在 low 档额外增加一条「签名被改坏应被拒绝」的对照断言，用于区分
  「`alg:none` 绕过了校验」与「该档完全不校验」。
- 越权增加一条断言：用 bob 订单的真实不可枚举 ID 攻击 high 档，排除
  「改用随机 ID 即等于修好」这一常见误解。
- CSRF 每条断言前重置场景状态，避免「本次是否成功」与「历史上是否成功过」混淆。

### 修复

- 反序列化场景的副作用判据不幂等，导致机读 PoC 成为一次性的。页面此前只在目录中
  出现**新**文件时报成功，而 POP 链写文件使用固定文件名，属于覆盖写入。第二次
  执行时目录不会出现新文件，成功文案不再打印，依赖该文案匹配的
  `pocs/vulnlab-php-unserialize-pop-chain.yaml` 随之失效。

  原有的 75 条断言未能发现此问题，因为它们都通过 `_unserialize_probe()` 发请求，
  而该函数每次执行前会删除探针文件，总是制造出「首次写入」的条件。现改为只要目标
  文件内容匹配预期标记即报成功，无论本次是新增还是覆盖。另增 3 条断言，故意不清理
  探针文件，验证连续两次请求的第二次同样报成功。

- 反序列化场景的副作用检查先于析构函数执行。`low.php`、`medium.php` 与
  `high.php` 在 `unserialize()` 之后立即比对目录，但 `__destruct` 在脚本结束时
  才触发，此时响应已组装完毕，页面始终显示没有新文件。改为显式 `unset($obj)`，
  使引用计数归零并立即触发析构。

- `urlencode()` 与数组键的类型问题。PHP 会把数字形式的字符串键自动转为整数，
  在 `declare(strict_types=1)` 下传给要求 `string` 的参数会抛出 `TypeError`。
  越权场景的订单号映射表受此影响。

- 统一了 XSS 之外 5 个 high 档「修复要点总结」的措辞。

### 变更

以下平台行为均经实测确认，并记录在对应场景的 Writeup 中。它们影响 PoC 的写法，
因此在本版本中一并明确：

- PHP 内置服务器把工作目录设为被请求脚本所在目录（`/probe.php` 解析到 `html/`，
  `/unserialize/probe.php` 解析到 `html/unserialize/`）。这与 Apache、PHP-FPM
  的行为不同，影响所有依赖相对路径的逻辑。靶场因此把正确路径直接算出来显示在页面上，
  而不是让使用者猜测。
- 相对路径写错时会静默失败：`file_put_contents` 返回 `false`，而靶场代码抑制了
  该告警。既不报错也不抛异常，页面只显示没有新文件。
- `shell_exec` 与反引号运算符共用同一底层实现，因此反引号**不能**绕过
  `disable_functions`。它绕过的是基于文本的黑名单，这正是 SSTI medium 档演示的内容。

## [0.1.0] - 2026-09-18

首个版本，包含 5 个漏洞场景，每个均配齐四件套。

### 新增

**漏洞场景**

| 场景 | low | medium 的绕过点 | high 的修复 |
|---|---|---|---|
| SQL 注入 | 数字型 UNION 注入 | 黑名单双写绕过 | 参数化查询 |
| XSS | 无转义输出 | `<img onerror>` 绕过标签黑名单 | `htmlspecialchars` 输出编码 |
| 文件上传 | 零校验 → getshell | 伪造 Content-Type | 白名单 + 内容检查 + 重命名 + 禁执行 |
| SSRF | 无限制打内网 + `file://` | `localhost` 绕过字符串黑名单 | 解析后校验 IP + 防 DNS 重绑定 |
| 命令注入 | 直接拼接 → RCE | `&` 绕过（黑名单漏掉 Windows 分隔符） | 白名单 + `escapeshellarg` |

**教学元素**

- SQL 场景把服务端实际执行的 SQL 展示给学习者
- XSS 场景提供「服务端输出的 HTML」与「浏览器解析结果」双视图对照，使转义的作用
  可见
- SSRF 与命令注入场景提供逐层审计表格，展示每一层校验观察到的内容
- 每个 high 档附带修复要点总结，覆盖实践中常被追问的点

**基础设施**

- 模拟的内网服务（`html/internal/inner-service.php`），作为 SSRF 的攻击目标
- `start.sh` / `start.bat` 同时启动两个服务（靶场 8080、内网服务 8090）
- Docker 部署（Dockerfile + docker-compose，两个服务）
- GitHub Actions CI：PHP 7.3 / 7.4 / 8.1 三版本矩阵

**CI 安全属性回归**（`tests/verify_lab.py`，21 条断言）

断言的对象是安全属性而非功能：

- 每个 low 档的漏洞场景确实可打通
- 每个 medium 档的绕过点确实能绕过
- 每个 high 档打不动，且不牺牲功能（反向保护，防止过度修复）
- 全站页面无 PHP 致命错误

### 修复

- Docker 部署缺口：compose 此前只启动一个服务，而 SSRF 场景需要第二个内网服务，
  导致用 Docker 跑靶场时 SSRF 打不通。现拆成两个服务，并新增
  `VULNLAB_INTERNAL_BASE` 环境变量（容器内 `127.0.0.1` 指向容器自身，必须使用
  服务名）。
- PHP 版本兼容：`str_starts_with()`（PHP 8.0 引入）与箭头函数（PHP 7.4 引入）
  在 PHP 7.3 上会致命错误，而页面仅表现为该区块内容消失，`php -l` 完全正常。
  已改用兼容写法，并把 PHP 7.3 加入 CI 矩阵。
- Windows 上命令输出为空：系统命令输出为 GBK 编码，直接传给
  `htmlspecialchars`（默认按 UTF-8）会触发非法 UTF-8 序列，PHP 在这种情况下
  返回空字符串而非跳过非法字节，表现为命令输出栏为空，且不报错、不告警。
  新增 `command_output_html()` 统一处理（先转码，并以 `ENT_SUBSTITUTE` 兜底）。

### 变更

以下平台行为均经实测确认，并记录在对应 Writeup 中：

- SSRF：`127.1`、`0x7f000001`、`2130706433` 等 IP 变形写法在 Linux 上有效、
  Windows 上无效。
- 命令注入：Windows `cmd` 不识别 `;`，也不支持换行作为分隔符；`&` 是 cmd 的
  主要分隔符，在 Unix 上同样有效。

[Unreleased]: https://github.com/hediwen831-star/vulnlab/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/hediwen831-star/vulnlab/releases/tag/v0.1.0
