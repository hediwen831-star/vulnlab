# 更新日志

本文件记录本项目的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

**补齐 6 个场景，至今共 12 个**。每个都仍然是完整四件套（三档源码 + Writeup + 机读 PoC + 修复对照）。

### 新增

**漏洞场景**

| 场景 | low | medium 的绕过点 | high 的修复 |
|---|---|---|---|
| 文件包含（LFI） | 路径穿越读任意文件 | 黑名单单次替换 → 双写 `....//` 绕过 | 白名单映射 + 越界兜底 |
| PHP 反序列化 | 任意类 + 任意属性 | 类名黑名单 → 小写 `cache` 绕过（PHP 类名不区分大小写） | `allowed_classes` 白名单 |
| XXE | 内联实体读文件 | 实体声明挪到外部 DTD → 过滤器看不见但解析器会加载 | 禁外部实体加载器 + 不传 `LIBXML_NOENT` + 输入收窄 |
| JWT | `alg:none` 被当成「无签名可校验 → 放行」 | 算法判断写对了，但密钥是 `secret` → 爆破后伪造签名 | 算法白名单 + 高熵密钥（环境变量）+ `exp` 校验 |
| CSRF | 零防护 | 只查 `Referer` → 攻击者可用 `no-referrer` 去掉它，或用含目标域名的自己人域名通过子串匹配 | 会话绑定的一次性 token + Referer 作为补充 |
| SSTI | 表达式直接 `eval` | 黑名单挡括号 → 反引号运算符 `` `cmd` `` 绕过 | 不做表达式求值，改为变量白名单（整个文件没有 `eval`） |
| 越权（水平 / IDOR） | 查询里没有归属条件 | 归属校验写对了，但「我是谁」来自请求参数 `uid` | 身份只从服务端取 + 归属条件进查询 |

**这一批的共同主题**

前 6 个场景的绕过方式各不相同，但把 12 个放在一起看，浮现出同一条主线：

> **防护所依据的那个信息，如果是请求方可以自由决定的，那它就挡不住请求方。**

| 场景 | 防护依赖的信息 | 为什么不可靠 |
|---|---|---|
| SQL 注入 | 删掉关键字后的文本 | 删除动作把片段拼了回来 |
| 文件包含 | 删掉 `../` 后的路径 | 同上 |
| 反序列化 | 字符串里的类名 | PHP 找类时不区分大小写 |
| XXE | 请求体里的文本 | 文档 + 外部 DTD 才是解析器实际的输入 |
| JWT | `alg` 字段 | 算法由 token 自己声明 |
| CSRF | `Referer` | 客户端可以自愿不发，域名也可以自愿包含 |
| SSTI | 括号 | 反引号运算符不需要括号 |
| 越权 | `uid` 请求参数 | 身份由请求方自己声明 |

**基础设施**

- `html/lib/paths.php`：把「当前工作目录到目标目录的相对路径」算出来并显示到页面上。
  起因是实测发现 **PHP 内置服务器会把 CWD 切到「被请求脚本所在目录」**，
  于是 payload 里的相对路径写错时 `@file_put_contents` 会**静默失败** ——
  页面只显示「没有新文件」，看起来像漏洞不存在。
- `html/csrf/state.php`、`html/idor/store.php`：场景状态与演示数据（含「当前登录用户」的模拟）

**CI 安全属性回归**：21 条 → **73 条**

新增 52 条，每个新场景都配了「对照组 + 绕过点 + 反向保护」三类断言。几条设计上值得一提的：

- **XXE** 用靶场自带的固定内容演示文件做判据，而不是 `/etc/passwd` / `win.ini` ——
  后者在不同机器上不一定存在，会让「读到没有」变成对环境的判断
- **JWT** 在 low 档额外加了一条「签名被改坏应被拒绝」的对照断言 ——
  少了它，分不清是「`alg:none` 绕过了校验」还是「这个档压根不校验」
- **越权** 加了一条「**用 bob 订单的真实不可枚举 ID 打 high 档**」的断言 ——
  用来排除「改用随机 ID 就等于修好了」这个常见误解
- **CSRF** 每条断言前都重置场景状态 ——
  否则「这次成功了吗」和「以前成功过吗」分不清

### 修复

- **反序列化场景的副作用检查看不到变化**
  `low/medium/high.php` 在 `unserialize()` 之后立刻比对目录，
  但 `__destruct` 默认在**脚本结束时**才触发 —— 那时候响应已经组装完了，
  于是页面永远显示「没有新文件」，即使 payload 其实已经成功写盘。
  修法是显式 `unset($obj)` 让引用计数归零、析构立刻执行。

- **XSS 之外的 5 个 high 档也补上了「修复要点总结」的措辞统一**

- **`urlencode()` / 数组键的类型问题**
  PHP 会把「数字形式的字符串键」自动转成整数，而 `declare(strict_types=1)` 下
  把它传给要求 `string` 的参数会直接抛 `TypeError`。
  越权场景的订单号映射表踩了这个坑（`$realId = $plain` → `(string) $plain`）。

### 记录在案的平台差异（新增）

- **PHP 内置服务器的当前工作目录是「被请求脚本所在目录」**
  （实测：`/probe.php` → `html/`，`/unserialize/probe.php` → `html/unserialize/`）。
  这与 Apache / FPM 的行为不同，会影响所有依赖相对路径的逻辑。
  靶场因此把「该填什么路径」直接算出来显示在页面上，而不是让使用者去猜。

- **相对路径写错时的静默失败**
  `file_put_contents` 失败只返回 `false`，而靶场代码里用了 `@` 抑制报错 ——
  没有告警、没有异常，只有一个「没有新文件」。
  这类问题比语法错误难排查得多：**语法错误会告诉你哪里错了，静默失败不会。**

- **`shell_exec` 与反引号运算符走的是同一个底层实现**
  因此反引号**不能**用来绕过 `disable_functions`；
  它绕过的是**基于文本的黑名单**（SSTI medium 档演示的就是这一层）。

---

## [0.1.0] - 2026-09-18

首个版本。**5 个漏洞场景，每个都凑齐四件套**（三档源码 + Writeup + 机读 PoC + 修复对照）。

### 新增

**漏洞场景**

| 场景 | low | medium 的绕过点 | high 的修复 |
|---|---|---|---|
| SQL 注入 | 数字型 UNION 注入 | 黑名单双写绕过 | 参数化查询 |
| XSS | 无转义输出 | `<img onerror>` 绕过标签黑名单 | `htmlspecialchars` 输出编码 |
| 文件上传 | 零校验 → getshell | 伪造 Content-Type | 白名单 + 内容检查 + 重命名 + 禁执行 |
| SSRF | 无限制打内网 + `file://` | `localhost` 绕过字符串黑名单 | 解析后校验 IP + 防 DNS 重绑定 |
| 命令注入 | 直接拼接 → RCE | `&` 绕过（黑名单漏掉 Windows 分隔符） | 白名单 + `escapeshellarg` |

**教学元素**

- SQL 场景把「服务端实际执行的 SQL」摊开给学习者看
- XSS 场景提供「服务端输出的 HTML」与「浏览器解析结果」**双视图对照** ——
  让「转义到底改变了什么」变得可见
- SSRF / 命令注入场景提供**逐层审计表格**，展示每一层校验看到了什么
- 每个 high 档都附带「修复要点总结」，覆盖实践中最常被追问的点

**基础设施**

- 模拟的「内网服务」（`html/internal/inner-service.php`），作为 SSRF 的攻击目标
- `start.sh` / `start.bat` 同时起两个服务（8080 靶场 + 8090 内网服务）
- Docker 部署（Dockerfile + docker-compose，两个服务）
- GitHub Actions CI：PHP 7.3 / 7.4 / 8.1 三版本矩阵

**CI 安全属性回归**（`tests/verify_lab.py`，**22 条断言**）

守的不是功能，是**安全属性**：

- 每个 low 档的漏洞场景**确实能打通**
- 每个 medium 档的绕过点**确实能绕过**
- 每个 high 档**打不动**，且**不牺牲功能**（反向保护，防止过度修复）
- 全站页面无 PHP 致命错误

### 修复

- **Docker 部署缺口**：compose 之前只起一个服务，而 SSRF 场景需要第二个
  「内网服务」—— 用 Docker 跑靶场时 SSRF 会打不通。
  现拆成两个服务，并新增 `VULNLAB_INTERNAL_BASE` 环境变量
  （容器内必须用服务名，因为容器里的 `127.0.0.1` 指向容器自己）
- **PHP 版本兼容**：`str_starts_with()`（PHP 8.0 引入）和箭头函数 `fn() =>`
  （PHP 7.4 引入）用在 PHP 7.3 上会 Fatal error，而页面只表现为「那一块内容消失」，
  `php -l` 完全正常。已改用兼容写法，并把 PHP 7.3 加进 CI 矩阵
- **命令输出为空**：Windows 上系统命令输出是 GBK 编码，
  直接丢给 `htmlspecialchars`（默认按 UTF-8）会触发「非法 UTF-8 序列」，
  而 PHP 在这种情况下**返回空字符串**而不是跳过非法字节 ——
  表现为「命令输出那一栏莫名其妙是空的」，不报错不告警。
  新增 `command_output_html()` 统一处理（先转码 + `ENT_SUBSTITUTE` 兜底）

### 记录在案的平台差异

这些是实测结论，都写进了对应的 Writeup：

- **SSRF**：`127.1` / `0x7f000001` / `2130706433` 等 IP 变形写法
  **Linux 有效、Windows 无效**（Linux 的 `inet_aton` 支持这些历史写法）
- **命令注入**：Windows cmd 不认 `;`、不支持换行作分隔符；`&` 才是 cmd 的主分隔符。
  而 `&` 在 Unix 上同样有效

> **payload 的有效性依赖目标环境** —— 这本身就是值得记录的知识点。

---

[Unreleased]: https://github.com/hediwen831-star/vulnlab/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/hediwen831-star/vulnlab/releases/tag/v0.1.0
